var express = require('express');
var router = express.Router();
var fs = require('fs');
var path = require('path');

/* GET home page. */

//router.get('/', function(req, res) {
//  res.status(200).send(path.join(process.cwd(), 'public', 'index.html'));
//});

module.exports = router;
